{
  "name": "shared",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm --filter backend build:self && tsc",
    "build:self": "pnpm --filter backend build:self && tsc",
    "dev": "pnpm --filter backend build:self && tsc --watch"
  },
  "dependencies": {
    "@orpc/contract": "^1.11.2",
    "backend": "workspace:*",
    "drizzle-zod": "^0.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3"
  }
}
