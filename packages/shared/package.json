{
  "name": "shared",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --external @orpc/contract --external zod",
    "dev": "tsup src/index.ts --format esm --dts --external @orpc/contract --external zod --watch"
  },
  "dependencies": {
    "@orpc/contract": "^1.11.2",
    "backend": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsup": "^8.5.1",
    "typescript": "^5.5.3"
  }
}
