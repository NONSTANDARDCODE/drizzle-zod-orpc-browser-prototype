import { router } from './src/router.ts';

console.log('Router keys:', Object.keys(router));
console.log('createUser type:', typeof router.createUser);
console.log('createUser keys:', Object.keys(router.createUser));
